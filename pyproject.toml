[project]
name = "raining-characters"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "blessed>=1.20.0",
    "requests>=2.32.3",
]

[dependency-groups]
dev = [
    "nuitka>=2.0.0",
]
